syntax = "proto3";

package inner;
option go_package="./inner";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Inner {
  rpc Ping(Request) returns(Response);
}

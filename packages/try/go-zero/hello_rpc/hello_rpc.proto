syntax = "proto3";

package hello_rpc;
option go_package="./hello_rpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Hello_rpc {
  rpc Ping(Request) returns(Response);
}
